apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
    name: roboshop-dr
    region: us-east-1
managedNodeGroups:
  - name: roboshop-dr
    instanceType: "t3.micro"
    desiredCapacity: 6 #  by default this value is 3