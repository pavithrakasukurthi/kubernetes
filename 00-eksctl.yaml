apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
    name: todo-stage
    region: us-east-1
managedNodeGroups:
  - name: todo-stage 
    instanceType: "t3.small"
    desiredCapacity: 3 #  by default this value is 3