apiVersion: v1
kind: Service
metadata:
  name: lb-demo
spec:
  type: LoadBalancer 
  selector:
    project: roboshop
    environment: stage
    component: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80