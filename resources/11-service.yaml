apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: roboshop
spec:
  selector:
    project: roboshop
    environment: stage
    component: frontend
  ports: 
   - protocol: TCP
     port: 80
     targetPort: 80